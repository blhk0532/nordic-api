{
    "private": true,
    "scripts": {
        "build:js": "esbuild resources/js/workspace-tabs.js --bundle --outdir=resources/dist --minify --format=esm",
        "build:css": "cp resources/css/workspace-tabs.css resources/dist/workspace-tabs.css",
        "build": "npm run build:js && npm run build:css"
    },
    "devDependencies": {
        "esbuild": "^0.25.0",
        "sortablejs": "^1.15.0"
    }
}
